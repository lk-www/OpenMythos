torch>=2.1.0
transformers>=4.40.0
# datasets>=2.18.0
pytest>=7.0.0
# torch>=2.11.0
datasets>=3.6.0
loguru>=0.7.3
open-mythos

# optional — enables Flash Attention 2 in GQAttention (requires CUDA + build tools)
# flash-attn>=2.8.3
